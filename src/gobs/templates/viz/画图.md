# 画图

离线工具：[[draw.html]]（库内路径 `80_meta/gobs-viz/draw.html`）

Obsidian 里点这个 html → 用默认浏览器打开（或 Copy path）。encode 课用它画「词在看谁」和「没有它印象包被挤」。画完点「复制到笔记」把 Mermaid/ASCII 贴回领域卡「双编码」节。map 课用「地图」页，导出贴进「## 领域地图」。

一次只画一张图、一个组块。不上网，不统计。中文界面，按钮够大。一次最多点亮 4 个词。
