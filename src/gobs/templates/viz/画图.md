# 画图

**双编码**：过程必须能看见。图给成年人：先准，再通俗。不是绘本，不是幼儿园插画。

**禁止**用图像生成模型出图。

跟 AI 上课本来就要联网。调用库才是正路，不限离线 HTML。

优先顺序：

1. **库图（主路径）**：matplotlib / graphviz。当场跑
   `python 80_meta/gobs-viz/draw.py seq-vs-attn --out PATH.png`
   或 `python 80_meta/gobs-viz/draw.py coref --out PATH.png`
   **过程随时间展开时，优先动态图**（gif / matplotlib.animation / 可点的 html）：
   `python 80_meta/gobs-viz/draw.py process --out PATH.gif`
   静态 PNG / Mermaid 仍可用。图跟讲解同一轮出现，链回领域卡。
2. **讲解里的 Mermaid**：当场画「怎么走 / 谁连谁」，跟讲一起出现，不要只在课后补。
3. **ASCII 兜底**：这轮库图还没好，先用 ASCII 把过程画出来，不要空讲。
4. **可选本页工具**：[[draw.html]] 还在。适合点词、画「看谁」。能用就用，不是主路径。

encode 课画当前那 1 个组块。map 课画当前节点（拆/留必须左右对照出现在同一张）。一次一张。
